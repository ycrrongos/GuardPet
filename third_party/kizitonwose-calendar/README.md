# kizitonwose/Calendar

- Upstream: https://github.com/kizitonwose/Calendar
- License: MIT
- Usage in 守伴: Gradle dependency `com.kizitonwose.calendar:view:2.10.1` embedded in `:app` (`ScheduleActivity` month grid). Not a separate APK.
- Product data / AI / habit rules remain in GuardPet (`DayScheduleStore`, `ScheduleLlmClient`, `HabitGuardian`).
